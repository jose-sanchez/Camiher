<Window x:Class="DragAndDrop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525" AllowDrop="True" Drop="OnDrop">
    <Grid>
  
        <DockPanel Height="272" HorizontalAlignment="Left"  Name="dockPanel1" VerticalAlignment="Top" Width="200" >
            <ListView DockPanel.Dock="Left" x:Name="ImageList" DisplayMemberPath="Name" >
                <!--<ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock>
                            
                        </TextBlock>
                    </DataTemplate>
                </ListView.ItemTemplate>-->
            </ListView>
                <Grid DockPanel.Dock="Right">
                <Image x:Name="imageViewer"/>
            </Grid>
        </DockPanel>
    </Grid>
</Window>
